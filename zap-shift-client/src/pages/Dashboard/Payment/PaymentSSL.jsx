import React from 'react';
import PaymentFormSSL from './PaymentFormSSL';

const PaymentSSL = () => {
    return (
        <div>
            <PaymentFormSSL/>
        </div>
    );
};

export default PaymentSSL;